n=c(1:4)
selector=c(1:4)
tmp <- colsplit(x, pattern=p, names=n)[selector]
tmp
typeof(tmp)
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
tmp3 <- vapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1), selector=c(1:4), bind=FALSE)
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:2), selector=c(1:4), bind=FALSE)
m
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:2), selector=c(1:4), bind=FALSE)
tmp3
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1), bind=FALSE)
tmp3
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
sapply(tmp3, split_n_bind)
typeof(tmp3)
str(linessplit)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=0, bind=FALSE)
tmp3
split_n_bind <- function(x, p="\\|",n=c(1,2), selector=c(1), bind=TRUE){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
if(bind){
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}else{
tmp
}
}
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
split_n_bind <- function(x, p="\\|",n=c(1,2), selector=c(1), bind=TRUE){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
if(bind){
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}else{
tmp
}
}
tmp3 <- sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE)
tmp3
typeof(tmp3)
tmp3 <- data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE))
tmp3
tmp3 <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE))
tmp3
lines
lines3 <- character(0)
x <- lines
p="\t"
n=c(1:4)
selector=c(1:4)
tmp <- colsplit(x, pattern=p, names=n)[selector]
tmp
typeof(tmp)
lines
lines3 <- character(0)
x <- lines
p="\t"
n=c(1:4)
selector=c(1:4)
tmp <- colsplit(x, pattern=p, names=n)
tmp
lines
x <- lines
x
tmp <- colsplit(x, pattern=p, names=n)
tmp
?sapply
tmp3 <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, use.names=FALSE))
tmp3
tmp3 <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, use.names=FALSE))
tmp3 <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, use.names=FALSE))
tmp3 <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
tmp3
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1,2), selector=c(1), bind=TRUE){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
if(bind){
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}else{
tmp
}
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
#linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- as.data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
lines
x <- lines
p="\t"
n=c(1,4)
selector=c(1)
bind=FALSE
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
tmp
x <- lines
p="\t"
n=c(1:4)
selector=c(1)
bind=FALSE
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
tmp
linessplit <- data.frame(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
?as.data.frame()
linessplit <- (sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- (sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- (sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- (sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit <- (sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
linessplit <- t(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- t(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE))
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- as.data.frame(t(sapply(lines, split_n_bind, p="\t",n=c(1:4), selector=c(1:4), bind=FALSE, USE.NAMES=FALSE)))
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1,2), selector=c(1), bind=TRUE){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
if(bind){
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}else{
tmp
}
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[selector], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[selector]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[1:2]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[,1]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[,1:2]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[[1]]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[[2]]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[[1]]][1]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[[1]][1]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[[1]][1:2]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1:2)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[selector]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), selector=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[selector]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
str(linessplit)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)[sel]
rbind(lines3,c(unlist(tmp, use.names=FALSE)))
}
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
c <- sapply(linessplit, split_n_bind)
c
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
linessplit <- sapply(linessplit, split_n_bind)
linessplit
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
linessplit <- sapply(linessplit, split_n_bind)
linessplit
apply(matrix(1:4, 2), 1, identity)
sapply(matrix(1:4, 2), 1, identity)
apply(matrix(1:4, 2), 1, identity)
matrix(1:4, 2)
apply(matrix(1:4, 2), 1, identity)
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
lines[1]
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
lines[1]
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
linessplit <- sapply(linessplit, split_n_bind)
linessplit
linessplit[1]
linessplit[1,]
linessplit
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1")
lines[1]
# define split_n_bind
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
# first split by \t
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
# first use colsplit with pattern \t to split lines into columes
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
# first use colsplit with pattern \t to split lines into 4 columes
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
# first use colsplit with pattern "\t" to split lines into 4 columes seperate by "\t"
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
linessplit <- sapply(linessplit, split_n_bind)
linessplit
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1","E|5\tF|6\t0.7\t0.2")
lines[1]
# define split_n_bind
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
# first use colsplit with pattern "\t" to split lines into 4 columes named 1,2,3,4
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
# second use sapply split_n_bind with pattern "\\|" to split linessplit into 2 columes named 1,2, so that generate 2 other rows that contains separated datas.
# then select first separated data, that is the row 1:2 (on the other hand, the second separated data is the row 3:4)
linessplit <- sapply(linessplit, split_n_bind)
linessplit
