# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines[1]
linessplit <- strsplit(lines, split="\t")
linessplit[1]
line1 <- linessplit[1]
l1 <- strsplit(line1[1], split = "|")
typeof(line1)
l1 <- strsplit(line1[1], split = "|")
line1
l1 <- strsplit(line1[[1]], split = "|")
line1 <- linessplit[1]
typeof(line1)
line1
l1 <- strsplit(line1[[1]], split = "|")
l1
line1
l1 <- strsplit(line1[[1]], split = "\\|")
l1
l1[[3]]
l1[[1]]
lapply(linessplit, strsplit, split = "|", fixed = T)
l2 <- lapply(linessplit, strsplit, split = "|", fixed = T)
l2
l2[[1]]
l2[[1]][[1]]
l2[[1]][[2]]
l2[[1]][[3]]
l2[[1]][[4]]
l2[[1]][[1]]
l2[[1]][[1]][1]
l2[[1]][[2]][1]
l2[[1]][[3]][1]
l2[[1]][[4]][1]
l2 <- sapply(linessplit, strsplit, split = "|", fixed = T)
l2[[1]][[4]][1]
l2[[1]]
l2
l2 <- sapply(linessplit, strsplit, split = "|", fixed = T)
l2
l2[1,]
l2[,1]
linessplit
l2 <- sapply(linessplit, colsplit, split = "|", fixed = T)
library(reshape2)
l2 <- sapply(linessplit, colsplit, split = "|", fixed = T)
library(reshape2)
l2 <- sapply(linessplit, colsplit, pattern = "|")
l2 <- sapply(linessplit, colsplit, pattern = "|",names=c(1:2))
l2
print(l2)
l2[1,]
l2[,1]
library(reshape2)
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2[,1]
l2
l2[1]
l2[1,]
l2[,1]
l2[,]
l2[,2]
l2[1,]
l2[2,]
l2[1,]
linessplit
l2 <- sapply(linessplit, strsplit, split = "|", fixed = T)
l2[,1]
l2[1,]
l2[2,]
l2[3,]
l2[,]
l2[1,]
l2 <- sapply(linessplit, strsplit, split = "\\|", fixed = T)
l2[1,]
l2[,]
l2 <- sapply(linessplit, strsplit, split = "\\|")
l2[,]
l2[,1]
l2[,2]
l2[,3]
linessplit
library(reshape2)
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2[1,]
l2[2,]
l2[1,]
typeof(l2)
l2 <- 1apply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l2[,1]
l2[,2]
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2[,2]
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2[]
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
linessplit
library(reshape2)
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l3 <- unlist(l2)
l3
l2
l2[1]
l2[1][[1]]
l2[1][1]
l2[1,]
l2[[1]]
l2[[1]][1]
l3 <- unlist(l2)
l3
l3 <- lapply(l2, unlist)
l3
l2
l3
l3[1:4]
l4 <- lapply(l3, unlist)
l4
l4 <- sapply(l3, unlist)
l4
l4[,1]
l4[1:4,1]
l2
l3 <- sapply(l2, unlist)
l3
l3[1:4,1]
l3[1:4,2]
l3[1:4,]
l2
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l2[1,]
l2[1]
l2[1,]
l2[1,]
l3 <- sapply(l2, unlist)
l3[1:4,]
linessplit
library(reshape2)
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2[1,]
l3 <- sapply(l2, unlist)
l3
tmp <- colsplit(linessplit, pattern = "\\|",names=c(1:2))
tmp
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines[1]
linessplit <- strsplit(lines, split="\t")
linessplit[1]
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit[1]
linessplit
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l2[1,]
l2[2,]
l2[1,]
l2[1,1]
l2[,1]
l2[1,]
l2
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
tmp <- colsplit(linessplit, pattern = "\\|",names=c(1:2))
tmp
linessplit
library(reshape2)
tmp <- colsplit(linessplit, pattern = "\\|",names=c(1:2))
tmp
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
line1 <- linessplit[1]
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
tmp <- colsplit(linessplit, pattern = "\\|",names=c(1:2))
tmp
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines[1]
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
tmp <- colsplit(linessplit, pattern = "\\|",names=c(1:2))
tmp
linessplit
tmp <- colsplit(linessplit, pattern="|",names=c(1:2))
tmp
linessplit
library(reshape2)
tmp <- colsplit(linessplit, pattern="\\|",names=c(1:2))
tmp
linessplit
library(reshape2)
l2 <- lapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l2 <- sapply(linessplit, colsplit, pattern = "\\|",names=c(1:2))
l2
l2[1]
l2[,1]
l2[1:2,]
l2[1:4,]
l2[,1:4]
l2[,1:4]
l2[1,1:4]
tmp <- colsplit(linessplit, pattern="\\|",names=c(1:2))
tmp
# define split_n_bind
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
# first use colsplit with pattern "\t" to split lines into 4 columes named 1,2,3,4
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
# second use sapply split_n_bind with pattern "\\|" to split linessplit into 2 columes named 1,2, so that generate 2 other rows that contains separated datas.
# then select first separated data, that is the row 1:2 (on the other hand, the second separated data is the row 3:4)
linessplit <- sapply(linessplit, split_n_bind)
linessplit
typeof(linessplit)
# first use colsplit with pattern "\t" to split lines into 4 columes named 1,2,3,4
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
test <- split_n_bind(linessplit)
test
linessplit
linessplit[1]
linessplit[1,]
test <- split_n_bind(linessplit)
test
test <- split_n_bind(linessplit.T)
test <- split_n_bind(t(linessplit))
test
linessplit[1,]
linessplit
cbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
# define split_n_bind
library(reshape2)
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
cbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
test <- split_n_bind(t(linessplit))
test
test <- split_n_bind((linessplit))
test
test <- split_n_bind(t(linessplit))
test
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
test <- split_n_bind(t(linessplit))
test
split_n_bind <- function(x, p="\\|",n=c(1:2), sel=c(1)){
lines3 <- character(0)
tmp <- colsplit(x, pattern=p, names=n)
rbind(lines3,c(unlist(tmp[sel], use.names=FALSE)))
}
test <- split_n_bind(t(linessplit))
test
temp2 <- t(linessplit)
temp2
linessplit
temp2 <- t(linessplit)
temp2
test <- split_n_bind(temp2)
test
temp3 <- colsplit(temp2, pattern="\\|", names=n=c(1:2))
temp3
temp3 <- colsplit(temp2, pattern="\\|", names=c(1:2))
temp3
temp2 <- t(linessplit)
temp2
temp3 <- colsplit(temp2, pattern="\\|", names=c(1:2))
temp3
temp3[1]
# second use sapply split_n_bind with pattern "\\|" to split linessplit into 2 columes named 1,2, so that generate 2 other rows that contains separated datas.
# then select first separated data, that is the row 1:2 (on the other hand, the second separated data is the row 3:4)
linessplit <- sapply(linessplit, split_n_bind)
linessplit
linessplit
# first use colsplit with pattern "\t" to split lines into 4 columes named 1,2,3,4
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
linessplit
linessplit[1,]
test <- split_n_bind(linessplit[1,])
test
# second use sapply split_n_bind with pattern "\\|" to split linessplit into 2 columes named 1,2, so that generate 2 other rows that contains separated datas.
# then select first separated data, that is the row 1:2 (on the other hand, the second separated data is the row 3:4)
linessplit <- sapply(linessplit, split_n_bind)
linessplit
test
linessplit[1,]
linessplit
# first use colsplit with pattern "\t" to split lines into 4 columes named 1,2,3,4
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
str(linessplit)
nrow(linessplit)
linessplit
linessplit[1,]
test <- split_n_bind(linessplit[1,])
test
linessplit[1,]
temp3 <- colsplit(linessplit[1,], pattern="\\|", names=c(1:2))
temp3
temp3 <- rowsplit(linessplit[1,], pattern="\\|", names=c(1:2))
temp3 <- colsplit(linessplit[1,], pattern="\\|", names=c(1:2))
temp3
temp3 <- t(colsplit(linessplit[1,], pattern="\\|", names=c(1:2)))
temp3
temp3[1]
temp3[,1]
temp3[1,]
linessplit[1,]
temp3 <- t(colsplit(linessplit[1,], pattern="\\|", names=c(1:2)))
temp3[1,]
testfunc <- function(x){
t(colsplit(x, pattern="\\|", names=c(1:2)))[1,]
}
testfunc <- function(x){
t(colsplit(x, pattern="\\|", names=c(1:2)))[1,]
}
testfunc <- function(x){
return t(colsplit(x, pattern="\\|", names=c(1:2)))[1,]
}
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))[1,]
}
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[1,]
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[1,]
}
temp4 <- sapply(linessplit, testfunc )
temp4
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[1,]
}
linessplit
temp4 <- sapply(linessplit, testfunc )
temp4
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines[1]
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[1,]
}
temp4 <- sapply(linessplit, testfunc )
temp4
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[2,]
}
temp4 <- sapply(linessplit, testfunc )
temp4
testfunc <- function(x){
temp33 <- t(colsplit(x, pattern="\\|", names=c(1:2)))
temp33[1,]
}
temp4 <- sapply(linessplit, testfunc )
temp4
linessplit
temp4
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines
library(reshape2)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
split_n_remove <- function(x){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[1,]
}
split_n_remove
split_n_remove <- function(x){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[1,]
}
temp4 <- sapply(linessplit, split_n_remove )
temp4
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines
library(reshape2)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
split_n_remove <- function(x){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[1,]
}
temp4 <- sapply(linessplit, split_n_remove )
temp4
split_n_select <- function(x){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[1,]
}
temp4 <- sapply(linessplit, split_n_select )
temp4
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines
library(reshape2)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
split_n_select <- function(x){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[1,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
# ================================
# origin data
lines <- c("A|1\tB|2\t0.5\t0.4", "C|3\tD|4\t0.9\t1", "E|5\tF|6\t0.7\t0.2")
lines
library(reshape2)
linessplit <- colsplit(lines, pattern="\t", names=c(1,2,3,4))
linessplit
split_n_select <- function(x, sel=c(1,)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
split_n_select <- function(x, sel=(1,)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=(1,)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel="1,"){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel='1,'){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=[1,]){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(1)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(1)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(1)){
tmp <- t(colsplit(x, pattern="\\|", names=c(1:2)))
tmp[sel,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(1), pat="\\|", nam=c(1:2)){
tmp <- t(colsplit(x, pattern=pat, names=nam))
tmp[sel,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(2), pat="\\|", nam=c(1:2)){
tmp <- t(colsplit(x, pattern=pat, names=nam))
tmp[sel,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
split_n_select <- function(x, sel=c(1), pat="\\|", nam=c(1:2)){
tmp <- t(colsplit(x, pattern=pat, names=nam))
tmp[sel,]
}
temp4 <- sapply(linessplit, split_n_select)
temp4
